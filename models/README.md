# Models

This directory will contain model implementations, code, and related resources for the AI Brain project.

## Purpose

This directory is reserved for:
- Model implementations
- Neural network architectures
- Training scripts
- Model checkpoints (references only, not actual files)
- Evaluation code

## Note

Large model files should not be committed to the repository. Use this directory for code and documentation only.

